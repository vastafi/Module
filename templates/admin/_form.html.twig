{{ form_start(form) }}
    {% if "edit" in app.request.requestUri %}
{#        {% do form.code.setRendered() %}#}
        {{ form_widget(form.code, { 'attr': {'class': 'hidden-row'} }) }}
        {{ form_widget(form.code, {'value' : app.request.get('productCode')}) }}
    {% endif %}
    {{ form_widget(form) }}
<div class="mt-3">
    <button type="submit" class="btn btn-success">{{ button_label|default('Save') }}</button>

</div>

{{ form_end(form) }}