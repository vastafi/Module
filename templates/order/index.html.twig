{% extends 'base.html.twig' %}

{% block title %}Order index{% endblock %}

{% block body %}
    <h1>Order index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Items</th>
                <th>PaymentDetails</th>
                <th>Status</th>
                <th>ShippingDetails</th>
                <th>Total</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for order in orders %}
            <tr>
                <td>{{ order.id }}</td>
                <td>{{ order.items ? order.items|json_encode : '' }}</td>
                <td>{{ order.paymentDetails ? order.paymentDetails|json_encode : '' }}</td>
                <td>{{ order.status }}</td>
                <td>{{ order.shippingDetails ? order.shippingDetails|json_encode : '' }}</td>
                <td>{{ order.total }}</td>
                <td>
                    <a href="{{ path('order_show', {'id': order.id}) }}">show</a>
                    <a href="{{ path('order_edit', {'id': order.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('order_new') }}">Create new</a>
{% endblock %}
